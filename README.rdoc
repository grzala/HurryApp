This project is a Advanced Web Application course groupwork submission

This application uses open data provided by the British government to create a map of public toilets in England.
The application can point the user to the nearest toilet and supports ratings, comments and a mobile interface.

To run, download, cd into the folder and run "rake db:migrate", "rake db:seed", "rails s" and visit localhost:3000